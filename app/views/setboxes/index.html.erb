
<div class="jumbotron bg-light">
  <div class="row p-4">
    <div class="col-5">
      <h1 class="display-4">Hello, word!</h1>
      <p class="lead">你是忘記了，還是記不起來？</p>
      <hr class="my-4">
      <p>善用零碎時間，記住任何你想記住的事物。</p>
    </div>
    <div class="col-7">
      <img src="https://images.unsplash.com/photo-1508873699372-7aeab60b44ab?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=800&q=60" style="width:100%">
    </div>
  </div>
</div>

<div class="bg-white m-5">
  <div class="container">
    <div class="card-deck">
      <% @setboxes.each do |setbox| %>
        <div class="col-4 text-white p-4">
          <div class="cardshadow p-3">
            <h5 class="card-title text-dark">
              學習集：<%= link_to setbox.title, setbox_path(setbox) %>
            </h5>
            <p class="card-text text-dark">
              作者：<%= setbox.user.name %>
            </p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="search bg-light p-5">
  <div class="container text-center">
    <%= form_tag(search_setboxes_path, :method => "get") do %>
      <%= text_field_tag :search, 
          nil, 
          class: "form-control", 
          placeholder: "搜尋學習集", 
          style: "display:inline-block;width:75%;" 
      %> 
      <%= submit_tag "搜尋",
          :name => nil,
          class: "btn btn-outline-secondary" 
      %> 
    <% end %> 
  </div>
</div>