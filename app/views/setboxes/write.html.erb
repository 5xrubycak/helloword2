    <%= form_tag(answer_setboxes_path, :method => "get") do %>
    <% @setboxes.each do |setbox| %>
    <% setbox.cards.each do |card| %>
    <%= "單字： #{card.card_word}" %>
    <%= "單字： #{card.card_def}" if params[:write] == card.card_def %>

    <% end %>
      <%= text_field_tag :write, 
          nil, 
          class: "form-control", 
          placeholder: "請輸入答案", 
          style: "display:inline-block;width:75%;" 
      %> 
      <%= submit_tag "送出",
          :name => nil,
          class: "btn btn-outline-dark" 
      %> 
      <% end %>
    <% end %> 